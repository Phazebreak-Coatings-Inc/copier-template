name: alembic-cicd

on:
  push:

concurrency:
  group: alembic-cicd-${{ github.ref }}
  cancel-in-progress: true

jobs:
  generate:
    name: Generate
    if: github.ref != 'refs/heads/main'
    runs-on: ubuntu-latest
    permissions:
      contents: write
    steps:
      - uses: actions/checkout@v6
      - uses: astral-sh/setup-uv@v6
        with:
          enable-cache: true
      - run: uv python install
      - run: uv sync
      - run: uv run python -m models cicd

  drift-check:
    name: Drift Check (main)
    if: github.ref == 'refs/heads/main'
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v6
      - uses: astral-sh/setup-uv@v6
        with:
          enable-cache: true
      - run: uv python install
      - run: uv sync
      - run: uv run python -m migrations cicd --check
