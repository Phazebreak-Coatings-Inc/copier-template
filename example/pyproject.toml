[project]
name = "example-project"
version = "0.1.0"
requires-python = ">=3.14"
dependencies = []

[build-system]
requires = ["uv_build>=0.11.18,<0.12"]
build-backend = "uv_build"

[dependency-groups]
dev = [
    "copier>=9.18.1",
    "inflection>=0.5.1",
    "pytest>=9.1.1",
    "tomlkit>=0.15.1",
    "typer>=0.27.2",
]
