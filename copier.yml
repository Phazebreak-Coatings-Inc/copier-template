project_name:
  type: str 
  help: The name of your next copier-template. #If using 'uvx example, it should be example_project' TODO: hardcode this
  default: example_project

module_name:
  type: str
  default: "{{ project_name | lower | replace('-', '_') | replace(' ', '_') }}"
  when: false

package_name:
  type: str
  default: "{{ project_name | lower | replace('_', '-') }}"
  when: false

your_name:
  type: str
  help: Your first and last name.
  default: Some User

github_repo:
  type: str
  help: Your github repo in the style of 'user/repo' without https://github.com/ 

_subdirectory: template
_answers_file: .copier-template-answers.yml
_tasks:
  - "uvx copier-template repair" 
_exclude:
  - "pyproject.toml"
  - ".venv"
  - ".cache"
  - ".ruff_cache"
  - ".pytest_cache"
  - ".skylos"
  - "__pycache__"
  - "*.egg-info"
  - "*.py[co]"
  - "~*"
  - "*.bar"
  - ".env*"
  - ".terraform"
  - ".terraformrc"
  - "terraform.rc"
  - "*.tfstate"
  - "*.tfstate.*"
  - "*.tfplan"
  - "*.tfvars"
  - "*.tfvars.json"
  - "crash.log"
  - "crash.*.log"
  - "override.tf"
  - "override.tf.json"
  - "*_override.tf"
  - "*_override.tf.json"
